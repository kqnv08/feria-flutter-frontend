query saleListPage($saleCriteria:FilterCriteriaInfo!){
  saleListPage(saleCriteria:$saleCriteria){
    data{
      id
      total
      name
      createdAt
    }
    limit
    page
    totalPages
    totalRecords
  }
}